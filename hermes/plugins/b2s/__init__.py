"""BOOK → SKILL (b2s) — плагин-панели, без агентной части.

Панель в правом rail'е зовёт детерминированные маршруты ``/api/plugins/b2s/...``
(``dashboard/plugin_api.py``), а прозу глав по-прежнему пишет агент в чате.
``register()`` намеренно пустой: плагину не нужны ни инструменты, ни хуки — запись
в ``plugins.enabled`` нужна лишь как разрешение дашборду импортировать его backend
(``web_server_dashboard._plugin_api_mount_skip_reason``).
"""


def register(ctx):  # noqa: ARG001 — контракт загрузчика плагинов Hermes
    """Нет агентной части: назначение плагина — маршруты дашборда."""
    return None
