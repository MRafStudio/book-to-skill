# Journal — book-to-skill (форк MRafStudio)

Рабочий журнал проекта. Ведётся агентом по ходу работы, чтобы решения и грабли
не терялись между сессиями. Технические правила самого проекта — в `AGENTS.md`
внутри репозитория (это файл апстрима, мы его не переписываем).

**Здесь только живое:** правила, грабли, актуальное состояние, открытые вопросы.
Хроники заходов, разборы инцидентов, отвергнутые пути и детальные замеры —
в `ЖУРНАЛ-АРХИВ.md` (подгружать, когда нужна история конкретного решения).
Новое пишем так же: правило и граблю — сюда, рассказ «как это делалось» — туда.
Причина разделения: приложение режет контекст-файлы на 30 720 символов, и
распухший журнал попадал в контекст только частично.

## Назначение

Превращать техническую документацию (и книги) в скиллы Hermes: по запросу
подгружаемые, плотные, без сырого текста источника.

## Где что лежит

| Что | Путь |
|---|---|
| Проект (рабочий каталог) | `D:\.VS\Projects\BOOK-TO-SKILL\` |
| Вендорский клон (не трогаем) | `_upstream\` — virgiliojr94/book-to-skill |
| **Наш форк (работаем тут)** | `_fork\` — MRafStudio/book-to-skill |
| Наша ветка | `agent/url-dashboard` |
| Копия журнала для git | `_fork/HERMES.md`, `_fork/ЖУРНАЛ-АРХИВ.md` (канон - эти два файла в корне; после правки копировать в `_fork/` и коммитить вместе с кодом) |
| Скиллы Hermes ложатся в | `$HERMES_HOME/skills/<category>/<slug>/` = `D:\NEURO\Hermes\data\hermes\skills\...` |
| Обзорная записка (прошлый заход) | `КАК-ЭТО-РАБОТАЕТ.md` |
| Архив журнала (хроники, история) | `ЖУРНАЛ-АРХИВ.md` |

Совпадение форка и апстрима на старте: оба были на коммите `a6cad12`.

## Проверенные факты (не перепроверять заново)

1. **URL как вход — только через наш фетчер.** Апстримный `resolve_input_files()`
   сетевых вызовов не делает (URL → `File not found: ...\https:\docs.python.org\...`),
   поэтому URL качает наш `book_to_skill/fetcher.py` в локальный `.md`/`.txt`, а дальше
   по конвейеру идёт обычный локальный файл — апстримный `scripts/extract.py` править
   не надо (пайплайн апстрима не заменяем, а докармливаем).
2. **Скилл ни на что не ссылается.** Внутри только относительные ссылки на свои
   же файлы (`chapters/ch01-*.md`). Путь/URL источника живёт в `metadata.json`
   во временном каталоге и **удаляется на Step 10**. Страницы/цитаты не проверить —
   это осознанная дыра, наш кандидат на правку.
3. **Без `trafilatura` мусор сайта попадает в текст.** stdlib-парсер режет только
   `script/style/head`: в пробном прогоне в текст вошли «Theme / Auto / Light / Dark»,
   сайдбарное ToC, `Prev/Next`, лицензионный футер, `Created using Sphinx`.
   Одна страница дала 7 914 слов и **3 фиктивные «главы»**.
4. **Контроль есть, но он про параметры, а не про результат.** Гейты спеки: Step 1.5
   (technical/text), Step 2.5 (смета), Step 4 (reference/study), Step 5 (куда класть,
   перезапись), Step 9.5 (security-скан), Step 11 (публикация). Черновик до записи
   на диск не показывается — есть только режим «Analyze Only» (отчёт по начинке).
5. **Детерминированной функции «сделай скилл» нет.** Python только достаёт текст;
   главы/глоссарий/паттерны пишет агент в своём контексте. Значит «предпросмотр
   скилла» = черновая выдача агента, а не вывод скрипта.
6. `gh` CLI на машине **не установлен** → Step 11 (публикация в GitHub) не работает.
7. Экстракторы на машине: ✅ `pdftotext` (poppler, PDF-режим text), ✅ `trafilatura` 2.2.0
   и ✅ `beautifulsoup4` 4.15.0 (поставлены 2026-09-11); ❌ `docling` (technical-режим
   деградирует), ❌ Calibre (MOBI/AZW/AZW3 без фоллбэка).


> Перенос 2026-09-14: разделы «Решения», «Грабли» и «Состояние на 2026-09-12» сжаты
> здесь, их дословные тексты — в `ЖУРНАЛ-АРХИВ.md` (оглавление — в шапке файла).
>
> **Архив веду сам:** как только запас до лимита 30 720 падает ниже ~2 000 символов,
> дословные пункты уезжают в `ЖУРНАЛ-АРХИВ.md`, а в журнале остаётся сжатая суть
> (правило + ссылка). Так файл не начнёт резаться приложением и не потеряет кусок.

## Решения

- **Свой форк, апстрим не трогаем; новое - новыми файлами** (апстрим-обновления безболезненны).
- **Каскад стратегий - мини-плагины Python**: базовые `raw-markdown` (источник отдаёт `.md`) и `html-cascade` (`trafilatura` -> `bs4` -> stdlib); специфичные - новые `plugin_*.py`, подхват автоматический.
- **Таблицы получают строку-разделитель постобработкой**: `fix_md_tables` в `plugins/generic.py` правит вывод `trafilatura` (блоки `|`-строк от 2 колонок без разделителя), в заметки отчёта идёт `tables: added markdown separator rows`. Без разделителя markdown таблицу не видит: в панели это простыня с палками, а глоссарий по таблице даёт 0 терминов. Уже корректную таблицу не трогаем.
- **`matches(url) -> 0..1`** у каждого плагина: перебор по убыванию оценки, все попытки в отчёт.
- **Язык - переключатель в панели** (Русский / Английский / без изменений): чистый текст источника не переводится, язык - при генерации, имена API в оригинале (дословно - архив).
- **`description` скилла всегда английский** (`Use when <триггер>`, софт-лимит 60, `validate_skill.py --lens hermes`); русским - тело.
- **Предпросмотр черновика - тумблер, выключен по умолчанию**; модалка с ценой при включении. Показ бесплатен, платится генерация; перегенерация после правок - заново.
- **Мастер из ПЯТИ блоков**: 1 источник -> 2 анализ -> 3 реквизиты записи (категория + имя) -> 4 черновик (без записи) -> 5 запись в профиль. Открыт один блок (шапка руками); кнопка - правый низ футера («ДАЛЕЕ ->», у блока 5 - запись), причина неактивности - подписью. Гейты: 1->2 источник не пуст; 2->3 отчёт **про этот** источник (`report.chars != null`, `fetchedSrc === src`); 3->4 имя не пусто; 4->5 черновик в staging; «ДАЛЕЕ» блока 1 запускает разбор.
- **Реквизиты записи стоят блоком 3, а не в хвосте**: блок 3 знает имя до черновика, блок 4 - каталог, блок 5 считает долив или замену; имя и категорию в отпечаток входов разбора не пускаем.
- **Источник «уже markdown» - блок 2 пропускается, решает панель**: признак из строки, без сети - `.md`/`.markdown` у пути или `pathname` URL (`?raw=1`, якорь не мешают); шапка блока 2 - «Анализ MD файла - пропущен: файл уже markdown»; ядро вернуло HTML - блок 2 открывается; **`.txt` markdown НЕ считается**.
- **Шаг 1 при сбое ядра не уходит в чат**: тихий `sendIntent('rerun')` из catch снят (падало 30-секундное задание и запускалось повторно); авария - строка статуса + «⟳ Повторить», повторный вход гасит `busyRef`; action-кнопки блокируются при `isRunning`.
- **Подпись называет ДЕЙСТВИЕ, а не номер чужого блока** («черновик делает кнопка ✎ в этом блоке»); ссылка на блок обязана нести путь туда, иначе - имя кнопки.
- **После платного шага панель следит сама**: `watchDraft()` читает `/draft` каждые 5 с (дедлайн 15 мин), файлы снимают ожидание; `draftWait` в шапке блока 3 - «задание в чате - жду staging».
- **Черновик отделён от установки**: генерация в staging, показ -> правки, перенос в `$HERMES_HOME/skills/...`.
- **Ключ черновика ведёт ПОЛЕ источника**: `POST /resolve {src}` (локально, без сети и LLM) при вводе, не `draft_key` из `/state` (там ключ последнего прогона); смена ключа снимает сводку, черновик, план записи, статус установки; вотчер держится за КЛЮЧ и читает источник через ref; имя и категорию в отпечаток входов разбора не пускаем.
- **Имя скилла предлагается ПОСЛЕ разбора**: `suggested_name` из `/rerun` (эвристика ядра `suggest_skill_name`); первая ручная правка закрывает подстановку (`nameTouched`), подпись «подставлено по источнику - переписать можно тут же».
- **Загрузка не заменяет пайплайн апстрима**: фетчер создаёт `.md`/`.txt`, дальше неизменённый `scripts/extract.py`.
- **Локальный источник - родным путём апстрима, не фетчером**: `resolve_input_files()` сети не делает; фетчер локальный путь не берёт (баг `urlopen` -> `unknown url type: d`); путь берёт плагин `local-file` и делегирует в `book_to_skill.utils.extract_single_file` - формат: `SUPPORTED_EXTENSIONS` + магия `%PDF`, `PK` -> `mimetype`/`word/document.xml`. Своё - только не-utf8 (cp1251/cp866) и каталог (склейка `.md`/`.txt` по алфавиту).
- **Чужой движок сначала изучаем**: `config.py`, `utils.extract_single_file`, `dependencies.prepare_dependencies`, `cli.py`; признак дублирования - своя таблица расширений, свой «detect format», второй реестр парсеров.
- **Панель - штатный desktop-плагин Hermes** (`desktop-plugins/b2s/plugin.js` + REST `plugins/b2s/dashboard/plugin_api.py`), не HTML-виджет: кнопки собирают интент и шлют в сессию (RPC `prompt.submit`); `dashboard/widget.html` - автономный предпросмотр.
- **Имя скилла и slug источника разведены**: имя - как скилл называется и лежит (`skills/<категория>/<имя>/`), редактируемое, из `suggest_skill_name()` (приоритет: сегмент URL -> заголовок -> имя файла); slug (`docs-python-org-3-library-pathlib-html`) - метка сырья для файлов и `staging/`, read-only. **Имя - максимум три слова**: сайт 1 слово, теме 2; служебные слова (`po`, `iz`, `how`, `to`) и числовые хвосты (`-186253740`) выбрасываются; из темы - ГОЛОВА и самое длинное из ОСТАЛЬНЫХ, из заголовка - начало; версия сохраняется (`csharp-13`). Дословно - архив.
- **Панель НЕ предзаполняет имя**: поле пустое или из `ctx.storage`, хардкод `python-pathlib` из `useState` убран; эвристика - в ядре.
- **Пустое имя - обязательный вход, панель это ПОКАЗЫВАЕТ**: поле и подпись жёлтые (`WARN_YELLOW` = `#f59e0b`), «имя скилла не может быть пустым!», «ДАЛЕЕ» блока 1 погашена, тултип «нужно имя скилла: пустым не поставим», `next1` не пускает, если имя стёрли после; подпись «такого скилла нет - будет создан новый».
- **Рамка состояния - как у чипсы `DESCRIPTION.md`**: `border: '1px solid ' + WARN_YELLOW` (как в `catTone`); `borderColor` + `boxShadow: inset 0 0 0 1px` запрещены.
- **Дефолты виджета**: режим движка - `technical - таблицы и код`; галочка «Показывать предполагаемый скилл до записи» включена; цена - модалкой `askDraft()` при нажатии кнопки генерации/перегенерации, не тумблера.
- **Источник вводится полем** (URL или путь, файл/папка); две кнопки-ярлыка (`Button size='icon-xs'` 24×24, `shrink-0`; подписи «Выбрать файл» / «Вставить из буфера обмена» в `title`/`aria-label`) - цветной эмодзи `📂`/`📋` инлайном (`fontSize: 13, lineHeight: 1`).
- **Файл и буфер берут штатные двери Electron:** `window.hermesDesktop.selectPaths({ multiple: false, filters, title })` (IPC `hermes:selectPaths`, полные пути; фоллбэк `<input type=file>` + `getPathForFile` отдаёт только имя - оно в тост, путь просят руками) и `window.hermesDesktop.readClipboard()` (IPC `hermes:readClipboard` -> `clipboard.readText()`; фоллбэк `navigator.clipboard.readText()` отказывает при расфокусе). Двери - в `electron/preload.ts` и `src/global.d.ts`, плагин в главном окне (blob-`import()` в `runtime-loader.ts`); отказ, пустой буфер, отмена - тост `host.notify({ kind, message, detail })`. Дословно - архив.
- **Схема интента (≤500 символов, лимит моста `::preview`)**: `b2s <kind> | src= | name= | strat= | mode= | depth= | lang= | cat= | slug= | draft=0|1 | won= | junk= | model=` (замер на pathlib - 275; `src=` заменил `url=`). Ветка категории: `b2s desc | cat= | dmode=write|fix | desc=`: `write` - файла нет, `fix` - без frontmatter, прежний текст сохраняется телом.
- **Действия - один нумерованный блок из трёх шагов**: 1 «Прогнать источник заново» (`rerun`, кнопка у поля источника, файлы скилла не пишутся) -> 2 «Сделать черновик» (`draft`, платная генерация в `staging/`, одна кнопка на заход и переделку, подпись «Перегенерировать с учётом замечаний») -> 3 «Установить» (`install`, перенос в `skills/<категория>/<имя>/`, гаснет без черновика); «Разобрать черновик» (`review`) - в блоке черновика.
- **Уборка рабочих каталогов, но не молча**: черновик - `staging/<слаг источника>`, сырьё - `b2s_fetched/<слаг>.*`; установленный - архив с TTL (`STAGING_TTL_DAYS = 7` дней), свежие - лимит (`STAGING_KEEP = 5` сверх активного), `_probe*` не трогаем, `reason` называет причину. Двери: автоуборка при открытии панели `POST /prune {keep: 99, days: 0, apply: true}` (лишь TTL); строка «рабочие каталоги в staging» в блоке 3 (число файлов, «установлен», корзина - `POST /drop`); кнопка «Убрать лишнее» (`POST /prune` без `keep`). Пометку ставит `_mark_installed` в `metadata.json`; активный - слаг = `src` или `source.url`; каталоги по имени скилла защищены.
- **Реквизиты ЗАПИСИ - в блоке 3**: «Категория скилла» (с описанием), «Имя скилла», список скиллов категории; блок 1 требует один вход - источник. **Ядро дописывает `name:` в шапку `SKILL.md` при установке** (`_retitle_skill_md`, ДО копирования staging->target), иначе каталог разъезжается с шапкой.
- **Маркер авторства - `creator`/`created`/`updated` внутри `metadata.hermes`** (`_stamp_hermes_meta`, до копирования): `creator: BookToSkill`, `created` - один раз, `updated` - всегда; верхним уровнем - 3 warning линзы `hermes`, в `metadata.hermes` - 0. В промпт идёт только `- {name}: {description}` (`prompt_builder.py`, `available_skills` -> `index_lines`). **Метку ставит только ядро при установке через панель: чужим скиллам профиля (128 файлов `SKILL.md`, из них наших 3) ничего не дописываем** - `creator: BookToSkill` у скилла, собранного руками, был бы подлогом.
- **Служебные файлы ядра в скилл не едут**: `merge-plan.json` фильтрует `_SERVICE_FILES` (`ignore=` при копировании), в `staging` остаётся; из трёх скиллов профиля удалён.

## Грабли (проверено на себе)

- **JS плагина проверяй целиком, а не фрагментом.** Пропущенная `)` в `nameNote` унесла вкладку молча, тесты были зелёными.
- **«Проверка не прошла» не значит «не записали»: ядро пишет файлы ДО проверки.** Кнопка гаснет по `out.installed`.
- **Имя, не проходящее линзу (`^[a-z0-9][a-z0-9._-]*$`), - незаполненный вход, как пустое.** Панель ловит до записи.
- **Тест не берёт фикстуру из живого `staging/`:** уборка сносит черновики (`FileNotFoundError`).
- **Правка шапки после копирования = правка в никуда:** `_retitle_skill_md` после `shutil.copytree`.
- **Автоуборка не трогает свежий черновик:** панель зовёт `POST /prune` с `keep: 99`.
- **TTL установленного черновика - от УСТАНОВКИ, не от mtime.** Активный каталог - по `src`/`source.url`.
- **Черновик не ключуй по имени скилла, ключ - слаг ИСТОЧНИКА:** иначе правка имени уводит поиск.
- **`_draft_dirs()` прячет `_probe*`: план и установка сперва проверяют прямой путь** (`STAGING/<ключ>`).
- **Ключ черновика бери только из явного `src`:** иначе `do_draft('чужое-имя')` подставит чужой черновик.
- **Формат staging диктует ядро:** `validate_skill.py` берёт файл `SKILL.md`, глоссарий таблицей = 0 терминов, `tool.exfiltration_shape` - по обеим половинам.
- **Поле шапки наружу идёт БЕЗ кавычек YAML (`_fm_text` в `tools/api.py`).** Кавычки это синтаксис, а не текст: через хелпер идут и `do_draft`, и описание категории (раньше правило жило в одном месте из двух - в чипсе висело `"Use when ..."`). В шапке пиши как удобно (кавычки уместны при `:`). Сторож - `test_draft_desc.py`.
- **`:8090` - не Hermes** (llama.cpp Windows Manager): dashboard на динамическом порту, признак - 401 на `/api/plugins/b2s/*`.
- **`git credential fill` здесь ВИСНЕТ:** нужен `GCM_INTERACTIVE=never`, явный username и `GIT_TERMINAL_PROMPT=0` при пуше.
- **Тесты панели ловят смену раскладки - это их работа:** маркеры позиции перенацелить, а не удалять.
- **`trafilatura`: `include_formatting=False` молча убивает структуру** (22 заголовка -> 0), **версия 2.2.0 теряет хвост страниц** (55 938 против 66 167 у bs4), **а потеря хвоста съедает таблицы** (`Corresponding tools` была только в `.txt`) - полноту мерить сравнением и добирать вторым извлечением.
- **trafilatura ставит строку-разделитель таблиц НЕ всегда:** на синтетическом HTML сама, на живом Confluence-источнике (`docs.rkeeper.ru`) нет. Фикс такого класса проверяется на реальном источнике - фикстура врёт.
- **Скрытая кнопка читается как потеря:** «Показать весь текст» рисуется только при `truncated` (панель тянет первые 6 000 символов). Маленький источник показывается целиком и кнопки не имеет - это не обрезка; полноту подтверждает сверка с независимым извлечением (у `70366840`: 2 370 против 3 325 у bs4, разница - хром сайта).
- **`Path.write_text` на Windows переводит файл в CRLF** (после `read_text` с universal newlines): журнал обязан быть LF - писать `write_bytes`, сверять байты питоном. **`grep -c $'\r'` в MSYS врёт** (показывает CR на файле без них) - мерить `bytes.count(b'\r')`.
- **Русская локаль переводит `sc query`** («ИМЯ_СЛУЖБЫ»), состояние - `UNKNOWN`: спрашивать PowerShell `Get-Service`.
- **`HERMES_HOME` под песочницу остаётся в сессии:** `tools/sync_hermes.py` развернул панель в песочницу; сверять печатаемую цель.
- **Имя службы dashboard зависит от пути установки** («HermesGateway (D:_NEURO_Hermes)») - искать по префиксу.
- **`git push` без токена молча падает (exit -1):** брать токен и сверять `git ls-remote` с `git rev-parse HEAD`.
- **Цель слияния - не «нетронутый файл»:** такие файлы идут списком `touched`.
- **Утверждения исполнителей проверяй по первоисточнику** (grep по обоим извлечениям), иначе в скилл заедет реконструкция.
- **Виджет в чате (`::preview`) - предпросмотр, а не поверхность управления:** путь относителен рабочей папке (абсолютный `D:/...` битый), тему приложение не инжектит (свой фон и палитра), мост в сессию есть только в инлайн-кадре, рендер ненадёжен (рабочий путь - `desktop_preview(action=open)`); контекст агента такой HTML не жрёт - читается с диска через `srcdoc`. Дословно - архив.
- **MSYS-путь `/d/...` не транслируется нативному python:** `--out /d/...` дал каталог `D:\d\...`.
- **`pip` в venv Hermes отсутствует** - ставить через `uv pip install --python <venv-python>`.
- **`browser_exec` здесь не работает** (CDP дефолт-профиля запрещён с Chromium >=136) - свой headless Chrome `chrome_cdp.py`.
- **`dashboard/render.html` генерируемый и в `.gitignore`:** правки только в `dashboard/widget.html`.
- **Длинный URL распирает таблицу панели:** `table-layout: fixed` + `word-break` + `overflow-wrap`.
- **`--check-overflow` ловит `input` с длинным значением как ложное переполнение:** критерий - `docScroll == bodyScroll`.
- **Мелочи обрезки и потолков в панели (дословно - архив 2026-09-15):** сам `truncate` не режет - нужны `min-width: 0`, `overflow: hidden`, `text-overflow: ellipsis`, `nowrap` инлайном; `SelectValue` выкидывает `className`/`style` (обрезать span вокруг); потолок ставь на ЗОНУ текста, кнопку - строкой под ней, окна блочные (`flex`-колонка с потолком врёт по `scrollHeight`); `shrink-0` базовой кнопки перебивается только инлайном (`BTN_FIT`); в ряду кнопок один кегль; плашка кнопки = её кликабельная зона (`alignSelf: flex-start`); индикатор несёт состояние и `title`; нативный `<datalist>` не годится - своя подсказка; подписи-объекты - одна строка с обрезкой (подпись ломается уже на 160 px).
- **Элемент, чья позиция важна, не ставь в ряд с подписью без `nowrap`:** имя переносилось, ряд рос 16 -> 48 px.
- **Тултип кнопки несёт действие, а не подпись:** третий аргумент `Ell`/`cutSpan`/`fitLabel` и карта `TIP`.
- **Панель не пускает платный шаг в пустоту:** `disabled: !!busy || !hasDraft` + `TIP.reviewOff`.
- **Кнопка называет действие первого клика:** без плана «Предпросмотр», после - «Установить», у замены - «Подтвердить ЗАМЕНУ».
- **План записи обесценивается действием в блоках 1-3:** `dropPreview()` в `sendIntent`, `runRerun`, `runPlan` (исключение - `loadDraft(silent)`).
- **Список, склеенный в строку, не читается: единица списка - своя строка, итоги - вне прокрутки** (`＋`/`⟳`/`＝`).
- **В выводимом тексте нет длинного тире (U+2014):** 96 замен по литералам `plugin.js`; тест валит U+2014.
- **Подпись, которую просят «на кнопку», переносится целиком:** она становится `title` кнопки.
- **Панель ничего не читает сама: это пульт** - интент `b2s <kind> | src=… | cat=…` (<=500 символов), шаги `rerun`, `draft`, `install`, `review`.
- **Под выпадашкой категории - чипса с содержимым `DESCRIPTION.md`**, фон инлайном (`color-mix` от `BASE`).
- **У блока описания состояние несут рамка и знак:** зелёная, жёлтая (`WARN_YELLOW`), красная (`STOP_RED` - нет файла у пустой категории).
- **Подписи к объектам панели - одна строка и обрезка, никогда перенос:** путь давал 4 строки; приём - `Ell()` и `CHIP_FIT`.
- **Рамки полей рисует приложение, наша ручка - knob `--dt-input-border: '22%'`** (инлайн `borderColor` убил бы hover).
- **`Ell` - это props, а не элемент: в `children` его класть нельзя** - React #31 и панель не рисуется.
- **Описание пустой категории в промпт не попадает - и это не поломка:** группа появляется с первым `SKILL.md`.
- **Действие, ушедшее в чат, панель сама не отследит - нужен вотчер:** `watchDesc(cat, before)` сверяет `desc_state`.
- **Фон кнопок-действий - на обёртке `div` с `data-glass-raised`, не на кнопке** (`chipAction`), тон `fix` - `--ui-green` 20 %.
- **Список категорий - каталоги на диске:** пустой каталог (`networking`) - категория `(пусто)`; вложенные - по `DESCRIPTION.md`.
- **Категория = каталог, где `SKILL.md` лежит прямо;** в `loose` идёт только `SKILL.md` прямо в `skills/`.
- **`loose` - не категории:** данные остаются в ответе ядра, в панели не показываются.
- **Список категорий перечитывай при каждом открытии выпадашки:** Fast Refresh хранит состояние, и `networking` терялся.
- **Долив и замена:** `auto` разрешается в `append`/`create` и идёт в отчёт; бэкап `backups/` (`BACKUP_KEEP=5`), `metadata.json.sources[]` мержится.
- **Описание категории - описание в промпте агента:** читает `_read_category_descriptions`; состояния `ok`/`no-frontmatter`/`no-file`.
- **План по главам: Python считает карту, решает LLM** - `add`/`merge` (`merge_into`)/`rewrite` и `touched` в `merge-plan.json`.
- **Версии панели и REST - зеркалом `_fork/hermes/` через `tools/sync_hermes.py`** (`--check`, `--pull`); маршруты - после рестарта dashboard.
- **`DESCRIPTION.md` без frontmatter для Hermes не существует:** замер - `apple` без шапки, `devops` без файла (`tools/api.py categories`).
- **`SKILL.md` в фикстурах не называй с `_` или точки** - линза валит установку, это похоже на поломку установщика.
- **Путь для `probe_route.probe()` - без ведущего слэша:** с `"/state"` выходит `b2s//state` и `404`.
- **«Окно» с фоном нужно любой скролл-зоне:** `data-glass-raised` + `border: FIELD_LINE` + `backgroundColor: PANEL_BG`.
- **Источник без заголовков - главы режет смысл, а не H2.** У страницы может не быть ни одного заголовка (`headings: 0` в `report.json`), и правило «глава на раздел» тогда неприменимо: текст делится по сути (процесс / формат данных), а `coverage_note` и границы скилла объясняют, почему глав именно столько. Пример - черновик `interaction-workflow` (страница «Алгоритм взаимодействия», 0 заголовков) с двумя главами по смыслу.
- **Короткая страница не значит «потеряли»: сверяй живой текст.** 101 211 байт живой страницы дали 1 902 символа очищенного текста - это размер статьи, а не обрезка `trafilatura`; подтверждает `web_extract` по тому же URL. Разница - только потерянная гиперссылка (онлайн-конвертер `conv.darkbyte.ru`, домен сохранён в скилле). Так и пишется `coverage_note`: с числом, без домысла.
- **Расхождения внутри источника помечай, а не исправляй.** На «Алгоритме взаимодействия» коды операций даны как `172`/`122` в шаге 2 и `126`/`172` в шаге 7 (похоже на опечатку), а имя - `OpRight` в тексте против `OpRights` в XML. В скилл идут оба варианта с пометкой «расхождение источника»: «исправленный» текст потом не отличить от документации, а сборку владельца от чужих примеров.

## Тесты (регресс)

Файлы — в `_fork/tools/tests/`, запускаются как `python -B <файл>`. Полный прогон — 21 файл, все зелёные (числа сняты с живого прогона; у тестов, которые печатают счётчик сами, число не дублируем).

`test_md_tables.py` держит фикс таблиц: разделитель вставляется, корректная таблица не портится, одна колонка и одиночная строка не трогаются, конвейер `clean_html` пропускает вывод извлекателя через фикс (проверка подменой `trafilatura.extract`, иначе тест зависит от версии).

Панельные тесты вырезают функцию из живого `plugin.js` и исполняют её в node: проверяют поведение, а не текст. `test_plugin_syntax.py` разбирает файл ЦЕЛИКОМ (фрагментный тест синтаксической аварии в другом месте файла не видит), `test_pane_name.py` берёт выражение инициализации имени из живого файла.

Перечень файлов с числами проверок — в `ЖУРНАЛ-АРХИВ.md` (перенос 2026-09-15).

## Состояние

Панель-плагин b2s работает живьём: dashboard поднят, `/api/plugins/b2s/skills` → 200, `/api/plugins/b2s/plan` → 200; регресс в `tools/tests/` (панель, ядро, уборка staging, установка) зелёный.
Пайплайн долива закрыт: файловый план → план по главам → установка с бэкапом.
- Имя скилла, подхваченное из прошлого захода, приводится к нижнему регистру: жёлтая подсветка на восстановленном `license-Info` читалась как поломка панели (владелец: «я ничего не делал, а он ругается»). Сторож — `test_pane_name.py` (8).
- Служебные метки `creator`/`created`/`updated` в наших скиллах: `created` правится по первой установке из `install_log` (`metadata.json`), а не по времени последней перезаливки — иначе «когда скилл появился» врёт.
- Черновики RK7 XML (`cat=rk7xml-interface`) в работе. Установлены в профиль: `introduction`, `methods-of-executing-commands`, `requests-and-functions`, `getreflist-getrefdata`, `rkeeper-licenseinfo`. Собраны и проверены черновики (лежат в `staging/`, установку делает панель):
  - `interaction-workflow` - «Алгоритм взаимодействия»: права на операции в `OpRights`, base64 -> биты, обратный порядок; 7 файлов, 2 главы, 22 термина;
  - `data-exchange` - «Обмен данными»: каркас `RK7Query`/`RK7QueryResult`, разбор ответа справочника `RK7Reference`; 7 файлов, 2 главы, 50 терминов;
  - `retrieving-the-current-menu` - «Получение актуального меню»: два запроса (`MENUITEMS` + `GetOrderMenu`), связь по `Ident`, цена в копейках, остаток в тысячных; 10 файлов, 5 глав, 72 термина;
  - `order-processing` - «Работа с заказами»: цепочка `GetOrderList`/`GetOrder`/`CreateOrder`/`SaveOrder`/`CloseVisit`/`VoidOrder`, скидки, комбо, курс, доставка; 11 файлов, 6 глав, 66 терминов.
  У всех четырёх: `validate_skill.py --lens hermes` 0 warning(s), `scan_generated_skill.py` чист, длинного тире нет, `api.py draft --src` отдаёт `has_draft: true` и `matched: slug`. Каждый идентификатор скилла сверяется с сырьём через `grep` (у `data-exchange` 70 из 70, у `retrieving-the-current-menu` 146 из 146, у `order-processing` 82 имени: 81 в сырье, одно - URL статьи-соседки из живой страницы).

## Открытые вопросы (нужны ответы владельца)

1. **Отложено:** странички отдаются готовым HTML или это JS-рендер (SPA)? Есть ли логин?
   Решено сначала доводить виджет, под странички — свои `plugin_*.py`.
2. Категория скилла и поле «назначение» (book / chapter / source) — где фиксируем.
   Текущий формат Hermes-скилла отдельного поля не имеет.
3. Нужна ли ссылка на первоисточник (`source_url`) во frontmatter сгенерированного
   скилла — как штатное расширение нашего форка. **Черновой ответ:** в top-level frontmatter
   не кладём (признанные ключи линзы Hermes — `name`, `description`, `version`, `author`,
   `license`, `metadata` и т. п.), а пишем в `metadata.hermes.source_url` + раздел «Источник»
   в теле + `metadata.json` в каталоге скилла. Проверено: валидатор молчит, `scan` проходит.
4. **Имя скилла из панели.** Панель подставляет слаг источника
   (`docs-python-org-3-library-pathlib-html`), а для скилла годится короткое имя
   (`python-pathlib`): каталог установки = имя скилла, длинный слаг в триггере мешает.
5. **Отложено владельцем (сказал «потом разберёмся»):** в категории вдруг обнаруживается
   файл `SKILL.md`. Тогда категория — уже не просто папка-группа, а скилл, и надо решить,
   что с этим делает панель: показывать как скилл, не давать выбрать как категорию,
   предупреждать или предлагать перенос. Пока поведение не задано — не выдумывать.
6. **Закрыто (2026-09-14).** Служебный `merge-plan.json` больше в скилл не уезжает:
   выбран вариант (а) — список `_SERVICE_FILES` фильтрует копирование
   (`ignore=`), план по файлам и список записанного; в `staging` файл остаётся, потому
   что панель его читает. Из трёх собранных скиллов профиля удалён
   (`getreflist-getrefdata`, `introduction`, `rkeeper-licenseinfo`). Заодно в шапку
   ставится маркер авторства `creator`/`created`/`updated` — см. «Решения».

## Для нас, Hermes

- **Панель кормит backend ПРИЛОЖЕНИЯ, а не служба dashboard - и это разные двери.**
  Панель b2s ходит в процесс, который Electron поднял сам: его порт виден в
  `data/hermes/logs/desktop.log` строкой `HERMES_BACKEND_READY port=65250` (сейчас
  `127.0.0.1:65250`, свой PID, старт 8:39). Служба `HermesGateway` (порт 9119,
  сетевой dashboard с basic_auth) в этом не участвует: её рестарт оживляет только
  её саму. Признак ошибки: `python tools/probe_route.py draft --post '{"name":"…","src":"…"}'`
  на 9119 отдаёт уже новый ответ (`has_draft: true`, `matched: meta`), а панель
  по-прежнему показывает «черновика нет» и гасит блок 4. Лечится **перезапуском
  Desktop** (`Start.bat`), не службы. Проверять сам сервер честно умеет `probe_route.py`
  (сравнивает путь с заведомо несуществующим контролем: 200+404 = смонтирован).
- Скиллы профиля: `HERMES_HOME=D:\NEURO\Hermes\data\hermes`, категории — подпапки
  `skills\<category>\`.
- Страницы и виджеты проверять своим headless Chrome:
  `python D:/NEURO/Hermes/scripts/py/chrome_cdp.py <file|url> --check-overflow --widths 300,460`
  (скилл `chrome-headless-cdp`). `browser_exec` на этой машине не поднимается.
- **Порт backend-а приложения берём из `desktop.log` (`HERMES_BACKEND_READY port=...`), но маршруты `/api/plugins/b2s/*` агенту недоступны: curl отдаёт `{"detail":"Unauthorized"}`.** Токен есть только у приложения, поэтому черновик/состояние проверяем ядром (`python -B tools/api.py draft --src <url>`), а не запросом в порт: тот же `do_draft` вернёт `has_draft`/`matched`, который увидит панель.
- Плагины десктопа: `desktop-plugins\<id>\plugin.js` (ESM, `jsx()` вместо JSX; экспорты SDK сверять
  по `apps/desktop/src/sdk/index.ts`). Канал «панель → чат» — RPC `prompt.submit`. Знание — скилл
  `hermes-desktop-html-previews`, файл `references/desktop-plugin-pane.md`.
- Проектные скиллы: `.hermes/skills` или `.agents/skills` + `hermes skills trust <project>`.
- `AGENTS.md` Hermes читает цепочкой от git-корня до cwd; `HERMES.md` — отдельный
  механизм, его апстрим никогда не создаст (значит, конфликтов не будет).
- **Язык ответов агента задаёт `SOUL.md` (identity slot #1), а не `display.language`.** Проверено
  по коду: `display.language` — это только i18n интерфейса (`agent/i18n.py`), на язык ответов
  модели он не влияет; ручной системный промпт — поле `agent.system_prompt` в `config.yaml`.
  Правило «только русский» стоит в начале `SOUL.md`, поэтому продублировано в конце: после
  длинного контекста и сжатия модели «плывут» к началу промпта. Срыв «отчёт на китайском» —
  это сбой самой модели (провайдер `qwen-3.6-35b` / `deepseek-v4-flash`), а не зашитое правило.
  Проверка чужого языка — CJK-скан по файлам проекта/профиля (диапазоны U+3000–303F,
  U+4E00–9FFF, U+FF00–FFEF); fullwidth `＋`/`＝` в подписях панели — ложное срабатывание.
- Файлы контекста сканируются на промпт-инъекции: попавшее под шаблон — **блокируется**,
  поэтому в них только обычная проза, без «служебных» инструкций.
- **Пуш в наш форк идёт токеном из `HERMES_HOME/.env`** (`D:\NEURO\Hermes\data\hermes\.env`,
  ключ `GITHUB_TOKEN`). В git-bash GCM креду не отдаёт: `git credential fill` по `github.com`
  пуст, хотя `git credential-manager github list` показывает `MRafStudio` и `x-access-token`,
  а `curl`-пуш без токена падает на `could not read Username`. Рабочая команда:
  `git push "https://${GITHUB_TOKEN}@github.com/MRafStudio/book-to-skill.git" agent/url-dashboard`,
  вывод прогонять через `sed "s/${GITHUB_TOKEN}/<TOKEN>/g"`. Льём ТОЛЬКО ветку
  `agent/url-dashboard` (`origin` - наш форк), в upstream не лезем; после пуша сверять
  `git ls-remote origin refs/heads/agent/url-dashboard` с локальным `HEAD`.
